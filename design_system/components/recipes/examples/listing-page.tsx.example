// =============================================================================
// EXEMPLO COMPLETO: Listing Page (Leads)
// =============================================================================
// Este arquivo é uma REFERÊNCIA de montagem — não é código executável.
// Mostra como todos os organismos, molecules e atoms do template listing-page
// se encaixam em uma única página React/Next.js.
//
// Para criar uma nova listing, copie este esqueleto e substitua:
//   - Entidade (Leads → Clientes, Produtos, etc.)
//   - Colunas da tabela
//   - Filtros do Filter Bar
//   - Stats dos Stat Cards
//   - Ações por row
//
// NOTA SOBRE COMPONENTES COMPARTILHADOS:
//   Sidebar e App Header aparecem inline neste exemplo para que ele seja
//   self-contained. Quando esses componentes já existirem implementados em
//   src/components/, importe-os em vez de duplicar o código.
//   Ex: import { Sidebar } from "@/components/layout/sidebar";
//       import { AppHeader } from "@/components/layout/app-header";
// =============================================================================

import { useState } from "react";
import {
  Users, UserPlus, Printer, Search, SlidersHorizontal,
  ArrowUpDown, Plus, ChevronDown, ChevronRight, ChevronLeft,
  Eye, Pencil, MessageCircle, LayoutDashboard, Kanban,
  BarChart3, LogOut, Settings, Bell, Menu, ArrowUpDown as SortIcon,
} from "lucide-react";

// Componentes UI (importar de src/components/ui/)
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Select } from "@/components/ui/select";
import { Badge } from "@/components/ui/badge";
import { IconButton } from "@/components/ui/icon-button";

// ---------------------------------------------------------------------------
// PAGE COMPONENT
// ---------------------------------------------------------------------------
export default function LeadsListingPage() {
  return (
    // Wrapper: sidebar + main
    <div className="flex h-screen bg-surface-base">

      {/* ================================================================= */}
      {/* SIDEBAR (organism) — catalog/organisms/sidebar.yaml               */}
      {/* ================================================================= */}
      <aside className="hidden md:flex flex-col w-64 h-full border-r border-border bg-surface-raised flex-shrink-0 z-30 shadow-sm">
        {/* Logo Block (molecule) */}
        <div className="p-6 pb-2">
          <div className="flex items-center gap-3">
            <div className="size-10 rounded-xl bg-action-primary flex items-center justify-center">
              <Kanban className="size-5 text-action-primary-fg" />
            </div>
            <div>
              <h1 className="text-lg font-bold text-text-primary tracking-tight">CRM</h1>
              <p className="text-xs text-text-secondary">Gestão de Leads</p>
            </div>
          </div>
        </div>

        {/* Navigation */}
        <nav className="flex-1 flex flex-col gap-2 px-4 py-6 overflow-y-auto">
          <a href="/dashboard" className="flex items-center gap-3 px-3 py-2 rounded-lg text-sm font-medium text-text-secondary hover:bg-surface-sunken transition-colors">
            <LayoutDashboard className="size-5" /> Dashboard
          </a>
          {/* Item ativo */}
          <a href="/leads" className="flex items-center gap-3 px-3 py-2 rounded-lg text-sm font-medium bg-action-primary text-action-primary-fg shadow-sm">
            <Users className="size-5" /> Leads
          </a>
          <a href="/pipeline" className="flex items-center gap-3 px-3 py-2 rounded-lg text-sm font-medium text-text-secondary hover:bg-surface-sunken transition-colors">
            <Kanban className="size-5" /> Pipeline
          </a>
        </nav>

        {/* Logout */}
        <div className="p-4 border-t border-border">
          <button className="flex items-center gap-3 w-full px-3 py-2 rounded-lg hover:bg-surface-sunken transition-colors text-left group">
            <LogOut className="size-5 text-text-secondary group-hover:text-action-ghost-fg" />
            <span className="text-text-secondary text-sm font-medium group-hover:text-action-ghost-fg">Sair</span>
          </button>
        </div>
      </aside>

      {/* ================================================================= */}
      {/* MAIN AREA                                                         */}
      {/* ================================================================= */}
      <main className="flex-1 flex flex-col overflow-hidden">

        {/* =============================================================== */}
        {/* APP HEADER (organism) — catalog/organisms/app-header.yaml       */}
        {/* =============================================================== */}
        <header className="h-16 flex items-center justify-between px-6 border-b border-border bg-surface-raised/80 backdrop-blur-md sticky top-0 z-20">
          <button className="md:hidden text-text-primary mr-4"><Menu className="size-6" /></button>
          <div className="flex-1 max-w-md hidden sm:flex">
            <div className="relative w-full">
              <Search className="absolute left-3 top-1/2 -translate-y-1/2 size-4 text-text-muted" />
              <Input placeholder="Buscar..." className="pl-9" />
            </div>
          </div>
          <div className="flex items-center gap-4 ml-4">
            <IconButton icon={Bell} />
            <IconButton icon={Settings} />
          </div>
        </header>

        {/* =============================================================== */}
        {/* CONTENT AREA (scrollable)                                       */}
        {/* =============================================================== */}
        <div className="flex-1 overflow-y-auto p-6">
          <div className="max-w-7xl mr-auto flex flex-col gap-6">

            {/* =========================================================== */}
            {/* PAGE HEADER — LISTING (organism)                             */}
            {/* catalog/organisms/page-header-listing.yaml                   */}
            {/* =========================================================== */}
            <div className="flex flex-col justify-between gap-6 sm:flex-row sm:items-end">
              <div className="flex flex-col gap-2">
                <h2 className="text-3xl font-bold tracking-tight text-text-primary">Leads</h2>
                <p className="text-text-secondary max-w-2xl">Gerencie todos os leads do seu funil de vendas.</p>
              </div>
              <div className="flex items-center gap-3">
                <Button variant="secondary"><Printer className="size-4" /> Imprimir</Button>
                <Button variant="primary"><UserPlus className="size-4" /> Novo Lead</Button>
              </div>
            </div>

            {/* =========================================================== */}
            {/* STAT CARDS GRID (organism)                                   */}
            {/* catalog/organisms/stat-cards-grid.yaml                       */}
            {/* =========================================================== */}
            <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
              {/* Stat Card (molecule) — catalog/molecules/stat-card.yaml */}
              <div className="bg-surface-raised rounded-xl border border-border p-5 shadow-sm">
                <p className="text-sm text-text-secondary">Total de Leads</p>
                <p className="text-2xl font-bold text-text-primary mt-1">1.284</p>
                <span className="text-xs text-feedback-success-fg font-medium">+15%</span>
              </div>
              <div className="bg-surface-raised rounded-xl border border-border p-5 shadow-sm">
                <p className="text-sm text-text-secondary">Leads Qualificados</p>
                <p className="text-2xl font-bold text-text-primary mt-1">847</p>
                <span className="text-xs text-feedback-success-fg font-medium">+8%</span>
              </div>
              <div className="bg-surface-raised rounded-xl border border-border p-5 shadow-sm">
                <p className="text-sm text-text-secondary">Taxa de Conversão</p>
                <p className="text-2xl font-bold text-text-primary mt-1">32,4%</p>
                <span className="text-xs text-feedback-danger-fg font-medium">-1,2%</span>
              </div>
              <div className="bg-surface-raised rounded-xl border border-border p-5 shadow-sm">
                <p className="text-sm text-text-secondary">Valor do Pipeline</p>
                <p className="text-2xl font-bold text-text-primary mt-1">R$ 2.4M</p>
                <span className="text-xs text-feedback-success-fg font-medium">+23%</span>
              </div>
            </div>

            {/* =========================================================== */}
            {/* FILTER BAR (organism)                                        */}
            {/* catalog/organisms/filter-bar.yaml                            */}
            {/* =========================================================== */}
            <div className="flex flex-col gap-4 rounded-xl border border-border bg-surface-raised p-4 shadow-sm xl:flex-row xl:items-center">
              <div className="relative flex-1 max-w-sm">
                <Search className="absolute left-3 top-1/2 -translate-y-1/2 size-4 text-text-muted" />
                <Input placeholder="Buscar por nome, email, empresa..." className="pl-9" />
              </div>
              <div className="grid grid-cols-1 gap-3 sm:grid-cols-2 lg:flex lg:flex-row">
                <Select options={["Todos os Status", "Novo", "Qualificado", "Proposta"]} />
                <Select options={["Todos os Responsáveis", "João", "Maria"]} />
                <Select options={["Todas as Origens", "Site", "Indicação", "WhatsApp"]} />
                <Button variant="secondary"><SlidersHorizontal className="size-4" /> Filtros</Button>
              </div>
            </div>

            {/* =========================================================== */}
            {/* SORT CONTROL (organism)                                      */}
            {/* catalog/organisms/sort-control.yaml                          */}
            {/* =========================================================== */}
            <div className="rounded-xl border border-border bg-surface-raised shadow-sm overflow-hidden">
              <button className="w-full flex items-center justify-between px-4 py-3 hover:bg-surface-sunken/50 transition-colors">
                <div className="flex items-center gap-2 text-sm font-semibold text-text-primary">
                  <ArrowUpDown className="size-4 text-text-secondary" />
                  Ordenação
                </div>
                <ChevronDown className="size-4 text-text-muted transition-transform duration-fast ease-standard" />
              </button>
            </div>

            {/* =========================================================== */}
            {/* DATA TABLE (organism)                                        */}
            {/* catalog/organisms/data-table.yaml                            */}
            {/* =========================================================== */}
            <div className="bg-surface-raised rounded-xl border border-border shadow-sm overflow-hidden">
              <div className="overflow-x-auto">
                <table className="min-w-full text-sm text-left">
                  <thead className="text-xs text-text-secondary uppercase bg-surface-sunken border-b border-border-subtle">
                    <tr>
                      <th className="py-3.5 pl-6 pr-3 font-semibold tracking-wide">Lead</th>
                      <th className="px-3 py-3.5 font-semibold tracking-wide">Empresa</th>
                      <th className="px-3 py-3.5 font-semibold tracking-wide">Valor</th>
                      <th className="px-3 py-3.5 font-semibold tracking-wide">Status</th>
                      <th className="px-3 py-3.5 font-semibold tracking-wide">Origem</th>
                      <th className="px-3 py-3.5 font-semibold tracking-wide">Data</th>
                      <th className="px-3 py-3.5 font-semibold tracking-wide text-right pr-6">Ações</th>
                    </tr>
                  </thead>
                  <tbody className="divide-y divide-border-subtle">
                    {/* Exemplo de row */}
                    <tr className="hover:bg-surface-sunken/80 transition-colors">
                      <td className="py-4 pl-6 pr-3">
                        <div className="flex items-center gap-3">
                          <div className="size-9 rounded-full bg-feedback-info-bg flex items-center justify-center text-xs font-bold text-feedback-info-fg">MS</div>
                          <div>
                            <p className="font-medium text-text-primary">Maria Silva</p>
                            <p className="text-xs text-text-secondary">maria@empresa.com</p>
                          </div>
                        </div>
                      </td>
                      <td className="px-3 py-4 text-text-primary">TechCorp</td>
                      <td className="px-3 py-4 text-text-primary font-medium">R$ 45.000</td>
                      <td className="px-3 py-4"><Badge intent="success">Qualificado</Badge></td>
                      <td className="px-3 py-4"><Badge intent="info">Site</Badge></td>
                      <td className="px-3 py-4 text-text-secondary">12/03/2026</td>
                      <td className="px-3 py-4 pr-6">
                        <div className="flex items-center justify-end gap-1">
                          <IconButton icon={Eye} size="sm" />
                          <IconButton icon={Pencil} size="sm" />
                          <IconButton icon={MessageCircle} size="sm" />
                        </div>
                      </td>
                    </tr>
                  </tbody>
                </table>
              </div>

              {/* ========================================================= */}
              {/* PAGINATION (organism)                                      */}
              {/* catalog/organisms/pagination.yaml                          */}
              {/* ========================================================= */}
              <div className="flex items-center justify-between border-t border-border-subtle px-6 py-3">
                <div className="hidden sm:flex sm:flex-1 sm:items-center sm:justify-between">
                  <div className="flex items-center gap-4">
                    <p className="text-sm text-text-secondary">Mostrando 1 a 10 de 1.284</p>
                    <div className="flex items-center gap-2 border-l border-border-subtle pl-4">
                      <label className="text-sm text-text-secondary whitespace-nowrap">Exibir</label>
                      <Select options={[10, 20, 50]} size="sm" />
                      <span className="text-sm text-text-secondary">por página</span>
                    </div>
                  </div>
                  <nav className="isolate inline-flex -space-x-px rounded-md shadow-sm">
                    <button className="px-3 py-2 text-sm bg-surface-raised text-text-secondary border border-border-subtle rounded-l-md hover:bg-surface-sunken">
                      <ChevronLeft className="size-4" />
                    </button>
                    <button className="px-3 py-2 text-sm bg-action-primary text-action-primary-fg border border-border-subtle">1</button>
                    <button className="px-3 py-2 text-sm bg-surface-raised text-text-secondary border border-border-subtle hover:bg-surface-sunken">2</button>
                    <button className="px-3 py-2 text-sm bg-surface-raised text-text-secondary border border-border-subtle rounded-r-md hover:bg-surface-sunken">
                      <ChevronRight className="size-4" />
                    </button>
                  </nav>
                </div>
              </div>
            </div>

          </div>
        </div>
      </main>
    </div>
  );
}
